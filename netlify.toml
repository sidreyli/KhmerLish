[build]
  command = "npm ci && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--prefer-offline"

# SPA redirects for React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
